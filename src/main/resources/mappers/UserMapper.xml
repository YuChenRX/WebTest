<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mapper.UserMapper">
    <resultMap id="BaseResultMap" type="com.pojo.User">
        <id column="uid" jdbcType="INTEGER" property="uid"/>
        <result column="upassword" jdbcType="VARCHAR" property="upassword"/>
        <result column="uname" jdbcType="VARCHAR" property="uname"/>
        <result column="uface" jdbcType="VARCHAR" property="uface"/>
        <result column="ulname" jdbcType="VARCHAR" property="ulname"/>

    </resultMap>

    <select id="getUser" parameterType="java.lang.Integer" resultMap="BaseResultMap">
    select * from user where uid=#{uid}
    </select>

    <select id="getAll"  resultMap="BaseResultMap">
        select * from user
    </select>

    <insert id="addUser" parameterType="com.pojo.User">
        insert into user(uname,upassword,uface,ulname)
        values(#{uname},#{upassword},#{uface},#{ulname})
    </insert>


    <!-- 根据id删除某个lie信息 -->
    <delete id="deleteUser" parameterType="java.lang.Integer">
        delete from user
        where uid = #{uid,jdbcType=INTEGER}
    </delete>

    <!-- 根据id更新biao信息 -->
    <update id="modifyUser" parameterType="com.pojo.User">
        update user set
        upassword=#{upassword}
        where uid = #{uid,jdbcType=INTEGER}
    </update>
    <select id="getUserByName" parameterType="java.lang.String" resultMap="BaseResultMap">
        select * from user where
        uname=#{uname}
    </select>
    <select id="getUserByNameAndPassword"  resultMap="BaseResultMap">
        select * from user where
            uname=#{uname} and upassword=#{upassword}
    </select>

</mapper>
