<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.web.web2022.dao.OrderDao">
    <insert id="create" useGeneratedKeys="true" keyProperty="id">
        insert into orders(id, created_at, state, price, owner_id)
        values (#{model.id}, #{model.createdAt}, #{model.state.ordinal}, #{model.price}, #{model.owner.id})
    </insert>
</mapper>